module.exports = {
  theme: {
    extend: {
      colors: {
        primary: '#A89BDA', // Example brand color
        secondary: '#F1E6FF', // Example secondary color
        accent: '#F9FAFB', // Example accent color
        // Add or modify colors to fit brand palette
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'], // Customize fonts for the design system
      },
      spacing: {
        '7': '1.75rem', // Custom spacing
      },
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
};